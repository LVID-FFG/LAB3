#pragma once

#include <vector>
#include <iostream>
using namespace std;
int len_in_bit(unsigned long long a);
vector<int> resheto_eratosfena();
unsigned long long num_exp(unsigned long a, unsigned long n, unsigned long p);
string test_millera_rabina(unsigned long long n, int k);